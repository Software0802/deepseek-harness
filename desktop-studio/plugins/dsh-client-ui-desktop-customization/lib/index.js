//#region lib/types/index.js
/** Host loader entry for the Desktop-only browser implementation. */
/** Host plugin body — all product behavior lives in the browser half. */
function apply() {}
//#endregion
export { apply };
